# ArrowListItem Component

The `ArrowListItem` component is used to display a list item with an arrow icon. It can be used as follows:

## Usage

To use the `ArrowListItem` component in your HTML file, include the following code:

```html
<div class="kai-al">
  <div class="kai-al-icon">
    <span class="kai-al-icon-unfocused"></span>
  </div>
  <div class="kai-al-line">
    <span class="kai-al-primary">Primary Text</span>
    <span class="kai-al-secondary">Secondary Text</span>
  </div>
</div>
```

## Properties

The `ArrowListItem` component does not have any specific properties. It relies on the CSS classes to style the component.

## Methods

The `ArrowListItem` component does not have any specific methods. It is a simple component that displays a list item with an arrow icon.
